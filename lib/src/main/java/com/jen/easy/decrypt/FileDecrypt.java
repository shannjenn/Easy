package com.jen.easy.decrypt;

/**
 * 作者：ShannJenn
 * 时间：2017/8/12.
 * 说明：文件加密解密
 */
public class FileDecrypt extends FileDecryptManager {
/*
    *//**
     * 解密
     *
     * @param strFile  文件路径
     * @param password 密码
     * @return 解密是否成功
     *//*
    @Override
    public boolean encrypt(String strFile, String password) {
        return super.encrypt(strFile, password);
    }

    *//**
     * 加密
     *
     * @param strFile  文件路径
     * @param password 密码
     * @return 加密是否成功
     *//*
    @Override
    public boolean decrypt(String strFile, String password) {
        return super.decrypt(strFile, password);
    }*/
}
